/* Mcaster1DAWCast — Default Theme
 * Uses system Fusion palette with polished structural styling.
 * Variables like ${accent} are substituted by ThemeEngine from theme.yaml.
 */

/* ── Global ────────────────────────────────────────────────────────── */

* {
    outline: none;
}

/* 1px dark edge on every structural widget. Border-radius 1 keeps corners
 * crisp but not sharp. Individual rules below can still override this. */
QGroupBox,
QFrame,
QDockWidget > QWidget,
QScrollArea,
QAbstractScrollArea,
QToolBar,
QStatusBar,
QMenuBar,
QMenu,
QTabWidget::pane,
QTabBar::tab,
QLineEdit,
QTextEdit,
QPlainTextEdit,
QComboBox,
QSpinBox,
QDoubleSpinBox,
QDateTimeEdit,
QProgressBar,
QTreeView,
QListView,
QTableView,
QHeaderView::section {
    border: 1px solid ${border};
    border-radius: 1px;
}

/* Splitter handles keep their own (thicker) styling — override. */
QSplitter::handle {
    border: none;
}

/* Dock widgets: 1px outer border so they're clearly distinct when docked
 * (including Media Library on the left, Effects / Video on the right, etc.),
 * with a visible title bar that shows close/float icons. */
QDockWidget {
    border: 1px solid ${border};
    titlebar-close-icon: url(:/icons/close.svg);
    titlebar-normal-icon: url(:/icons/undock.svg);
}
/* The content widget Qt hosts inside the dock body also gets a border so
 * the inner content region is clearly bounded — this is what was missing
 * for Media Library on the left pane. */
QDockWidget > QWidget {
    border: 1px solid ${border};
}
QDockWidget::title {
    background-color: ${secondary};
    border-bottom: 1px solid ${border};
    padding: 6px 10px;
    font-weight: 600;
    text-align: left;
}
QDockWidget::close-button,
QDockWidget::float-button {
    background: transparent;
    border: 1px solid transparent;
    padding: 0;
    icon-size: 12px;
}
QDockWidget::close-button:hover,
QDockWidget::float-button:hover {
    border-color: ${border};
    background: palette(light);
}

/* QPushButton gets its border from BevelButton's painter; leave unstyled
 * here so our custom buttons don't get a double frame. */

QMainWindow {
    background-color: ${primary};
}

/* (Unified dock widget styling lives earlier in this file.
 *  The duplicate QDockWidget block that used to live here was
 *  stripping the 1px border via QSS cascading. Removed.) */

/* ── Transport Bar ─────────────────────────────────────────────────── */

#TransportBar {
    background-color: ${transport_bg};
    border-top: 1px solid ${border};
    padding: 8px 12px;
}

#TransportBar QPushButton {
    min-width: 42px;
    min-height: 42px;
    border-radius: 21px;
    border: 1px solid ${border};
    background-color: ${button_bg};
    color: ${text_primary};
    font-size: 16px;
    font-weight: bold;
}

#TransportBar QPushButton:hover {
    border-color: ${accent};
}

#TransportBar QPushButton:pressed {
    background-color: ${accent};
}

#TransportBar QPushButton#playButton:checked {
    background-color: ${onair_green};
    color: white;
    border-color: ${onair_green};
}

#TransportBar QPushButton#recordButton {
    color: ${record_red};
    font-weight: bold;
}

#TransportBar QPushButton#recordButton:checked {
    background-color: ${record_red};
    color: white;
    border-color: ${record_red};
}

#TransportBar QLabel#timeDisplay {
    font-family: "JetBrains Mono", "SF Mono", "Courier New", "Menlo", monospace;
    font-size: 20px;
    font-weight: bold;
    color: ${accent};
    background-color: ${secondary};
    border: 1px solid ${border};
    border-radius: 6px;
    padding: 6px 14px;
    letter-spacing: 1px;
}

/* ── Timeline ──────────────────────────────────────────────────────── */

#TimelineWidget {
    background-color: ${timeline_bg};
    border: 1px solid ${border};
}

/* ── Track Headers ─────────────────────────────────────────────────── */

#TrackHeaderWidget {
    background-color: ${track_header_bg};
    border-right: 1px solid ${border};
    border-bottom: 1px solid ${border};
    padding: 6px 8px;
}

#TrackHeaderWidget QLabel {
    font-weight: 600;
    font-size: 12px;
}

#TrackHeaderWidget QPushButton {
    min-width: 26px;
    min-height: 20px;
    border-radius: 4px;
    border: 1px solid ${border};
    background-color: ${button_bg};
    font-size: 10px;
    font-weight: bold;
    padding: 2px 4px;
}

#TrackHeaderWidget QPushButton:hover {
    border-color: ${accent};
}

#TrackHeaderWidget QPushButton#muteButton:checked {
    background-color: ${mute_orange};
    color: white;
    border-color: ${mute_orange};
}

#TrackHeaderWidget QPushButton#soloButton:checked {
    background-color: ${solo_yellow};
    color: white;
    border-color: ${solo_yellow};
}

#TrackHeaderWidget QPushButton#recordArmButton:checked {
    background-color: ${record_red};
    color: white;
    border-color: ${record_red};
}

/* ── Mixer ─────────────────────────────────────────────────────────── */

#MixerWidget {
    background-color: ${secondary};
    border-top: 1px solid ${border};
}

QSlider::groove:vertical {
    width: 4px;
    background-color: ${fader_groove};
    border-radius: 2px;
}

QSlider::handle:vertical {
    height: 14px;
    width: 22px;
    background-color: ${fader_handle};
    border-radius: 7px;
    border: none;
    margin: 0 -9px;
}

QSlider::groove:horizontal {
    height: 4px;
    background-color: ${fader_groove};
    border-radius: 2px;
}

QSlider::handle:horizontal {
    width: 14px;
    height: 14px;
    background-color: ${fader_handle};
    border-radius: 7px;
    border: none;
    margin: -5px 0;
}

/* ── Menus ─────────────────────────────────────────────────────────── */

QMenuBar {
    background-color: ${primary};
    color: ${text_primary};
    border-bottom: 1px solid ${border};
    padding: 2px 0;
}

QMenuBar::item {
    padding: 6px 12px;
    border-radius: 4px;
}

QMenuBar::item:selected {
    background-color: ${accent};
    color: white;
}

QMenu {
    background-color: ${secondary};
    color: ${text_primary};
    border: 1px solid ${border};
    border-radius: 6px;
    padding: 6px;
}

QMenu::item {
    padding: 8px 28px 8px 12px;
    border-radius: 4px;
}

QMenu::item:selected {
    background-color: ${accent};
    color: white;
}

QMenu::separator {
    height: 1px;
    background-color: ${border};
    margin: 6px 10px;
}

/* ── Tabs ──────────────────────────────────────────────────────────── */

QTabWidget::pane {
    border: 1px solid ${border};
    background-color: ${primary};
    border-radius: 0 0 4px 4px;
}

QTabBar::tab {
    background-color: transparent;
    color: ${text_secondary};
    padding: 10px 18px;
    border: none;
    border-bottom: 2px solid transparent;
    margin-right: 2px;
    font-weight: 500;
}

QTabBar::tab:selected {
    color: ${text_primary};
    border-bottom: 2px solid ${accent};
}

QTabBar::tab:hover:!selected {
    color: ${text_primary};
}

/* ── Scrollbars ────────────────────────────────────────────────────── */

QScrollBar:vertical {
    width: 8px;
    background-color: transparent;
    border: none;
    margin: 2px;
}

QScrollBar::handle:vertical {
    background-color: ${border};
    border-radius: 4px;
    min-height: 24px;
}

QScrollBar::handle:vertical:hover {
    background-color: ${text_secondary};
}

QScrollBar:horizontal {
    height: 8px;
    background-color: transparent;
    border: none;
    margin: 2px;
}

QScrollBar::handle:horizontal {
    background-color: ${border};
    border-radius: 4px;
    min-width: 24px;
}

QScrollBar::handle:horizontal:hover {
    background-color: ${text_secondary};
}

QScrollBar::add-line, QScrollBar::sub-line,
QScrollBar::add-page, QScrollBar::sub-page {
    width: 0;
    height: 0;
    background: transparent;
    border: none;
}

/* ── Input Fields ──────────────────────────────────────────────────── */

QLineEdit, QTextEdit, QPlainTextEdit, QSpinBox, QDoubleSpinBox, QComboBox {
    border: 1px solid ${border};
    border-radius: 6px;
    padding: 6px 10px;
    selection-background-color: ${accent};
}

QLineEdit:focus, QTextEdit:focus, QPlainTextEdit:focus,
QSpinBox:focus, QDoubleSpinBox:focus {
    border-color: ${accent};
}

QComboBox {
    padding-right: 24px;
}

QComboBox::drop-down {
    border: none;
    width: 24px;
    border-left: 1px solid ${border};
}

QComboBox QAbstractItemView {
    border: 1px solid ${border};
    border-radius: 6px;
    selection-background-color: ${accent};
    padding: 4px;
    outline: none;
}

/* ── Buttons ───────────────────────────────────────────────────────── */

QPushButton {
    border: 1px solid ${border};
    border-radius: 6px;
    padding: 7px 18px;
    font-weight: 500;
    color: #1a1a1a;
    background-color: #f0f0f4;
}

QPushButton:hover {
    border-color: ${accent};
    background-color: #e4e4ea;
    color: #1a1a1a;
}

QPushButton:pressed {
    background-color: #d0d0d8;
    color: #1a1a1a;
}

QPushButton:disabled {
    color: #888;
    background-color: #f4f4f6;
}

/* ── Tooltips ──────────────────────────────────────────────────────── */

QToolTip {
    background-color: ${secondary};
    color: ${text_primary};
    border: 1px solid ${accent};
    padding: 6px 10px;
    border-radius: 6px;
    font-size: 12px;
}

/* ── Group Boxes ───────────────────────────────────────────────────── */

QGroupBox {
    border: 1px solid ${border};
    border-radius: 6px;
    margin-top: 12px;
    padding: 12px 8px 8px 8px;
    font-weight: 600;
}

QGroupBox::title {
    color: ${accent};
    subcontrol-origin: margin;
    left: 12px;
    padding: 0 6px;
    font-size: 12px;
}

/* ── Progress Bars ─────────────────────────────────────────────────── */

QProgressBar {
    border: 1px solid ${border};
    border-radius: 4px;
    text-align: center;
    font-size: 11px;
    height: 8px;
}

QProgressBar::chunk {
    background-color: ${accent};
    border-radius: 3px;
}

/* ── Splitters ─────────────────────────────────────────────────────── */

QSplitter::handle {
    background-color: ${border};
}

QSplitter::handle:horizontal {
    width: 1px;
}

QSplitter::handle:vertical {
    height: 1px;
}

QSplitter::handle:hover {
    background-color: ${accent};
}

/* ── Status Bar ────────────────────────────────────────────────────── */

QStatusBar {
    background-color: ${transport_bg};
    color: ${text_secondary};
    border-top: 1px solid ${border};
    font-size: 12px;
    padding: 2px 8px;
}

QStatusBar::item {
    border: none;
}

/* ── Tree / List Views ─────────────────────────────────────────────── */

QTreeView, QListView, QTableView {
    border: 1px solid ${border};
    border-radius: 6px;
    alternate-background-color: ${secondary};
    selection-background-color: ${accent};
    outline: none;
}

QTreeView::item, QListView::item, QTableView::item {
    padding: 4px 8px;
    border-radius: 3px;
}

QTreeView::item:selected, QListView::item:selected, QTableView::item:selected {
    background-color: ${accent};
    color: white;
}

QHeaderView::section {
    background-color: ${primary};
    border: none;
    border-bottom: 1px solid ${border};
    border-right: 1px solid ${border};
    padding: 6px 10px;
    font-weight: 600;
    font-size: 11px;
}

/* ── Check / Radio Boxes ───────────────────────────────────────────── */

QCheckBox, QRadioButton {
    spacing: 8px;
}

QCheckBox::indicator, QRadioButton::indicator {
    width: 16px;
    height: 16px;
    border: 1px solid ${border};
}

QCheckBox::indicator {
    border-radius: 4px;
}

QRadioButton::indicator {
    border-radius: 8px;
}

QCheckBox::indicator:checked, QRadioButton::indicator:checked {
    background-color: ${accent};
    border-color: ${accent};
}

QCheckBox::indicator:hover, QRadioButton::indicator:hover {
    border-color: ${accent};
}

/* ── Toolbar ───────────────────────────────────────────────────────── */

QToolBar {
    border: none;
    border-bottom: 1px solid ${border};
    padding: 2px 4px;
    spacing: 4px;
}

QToolBar::separator {
    width: 1px;
    background-color: ${border};
    margin: 4px 6px;
}

QToolButton {
    background-color: transparent;
    border: 1px solid transparent;
    border-radius: 6px;
    padding: 4px 8px;
    color: #1a1a1a;
}

QToolButton:hover {
    border-color: ${border};
    background-color: #e4e4ea;
    color: #1a1a1a;
}

QToolButton:pressed, QToolButton:checked {
    background-color: #d0d0d8;
    border-color: ${accent};
    color: #1a1a1a;
}
